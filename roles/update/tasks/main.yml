---
- name: Update distro
  apt:
    upgrade: safe
    update_cache: True
  register: rg

- name: Reboot a slow machine that might have lots of updates to apply
  reboot:
    reboot_timeout: 60
  when: rg.changed
